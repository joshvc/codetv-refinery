<ul>
  <% if Admin::DriversController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_drivers_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_driver_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::DriversController.sortable? and Driver.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Drivers"),
                 admin_drivers_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Drivers"),
                 admin_drivers_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>